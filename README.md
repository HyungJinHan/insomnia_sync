# insomnia_sync

## Usage
1. `POST`를 통해 생성
2. `GET`을 통해 생성한 하드웨어 스펙(or 디바이스) 목록 불러오기
3. 단일 `GET`의 경우, 앞서 모두 `GET`한 하드웨어 스펙(or 디바이스) 목록에서 `[0]`의 UUID를 참조하여 불러옴
4. `PUT`, `DELETE` 모두 단일 `GET`과 동일한 로직으로 작동
	- _`DELETE` 이벤트 실행 후, 모두 `GET` 이벤트 실행 필요 (단일 `GET` 이벤트 에러 발생으로 인함)_
